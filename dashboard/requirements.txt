Flask>=3.1.3
Flask-Cors>=6.0.4
requests>=2.33.0
pytest>=9.1.0
# Phase 3 (optional) : enable /admin/cohort/report.pdf
# requires native pango/cairo libs; route returns 503 if missing
# weasyprint>=60.0
