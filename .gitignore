# Build artifacts
node_modules/
.angular/
dist/
build/
*.log
*.pyc
__pycache__/
.pytest_cache/

# Secrets et tokens — NE JAMAIS COMMITTER
docker/.env
.env
.env.local
.env.*.local

# Dashboard runtime
dashboard/data/

# Upstream Juice Shop clone — lives next to this repo as a sibling, never inside.
# Documented in INSTALL.md (the overlay is applied to a vanilla clone).
juice-shop/

# Launcher runtime state (juice.ps1 / juice.sh)
.run/
.logs/

# Claude Code internal state
.claude/output/
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Caches IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Reference docs (large PDFs)
docs/*.pdf
